name: CI

on:
  push:
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Reconstruct verified CO payload
        run: python .co_complete_stage/reconstruct.py
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - name: Format reconstructed test
        run: ruff format tests/test_pathway_specification.py
      - run: ruff check .
      - run: mypy src
      - run: pytest --cov=aermodkit --cov-report=term-missing

  apply:
    if: ${{ github.event_name == 'pull_request' }}
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install "ruff>=0.5"
      - name: Reconstruct verified CO payload and clean staging
        run: python .co_complete_stage/reconstruct.py --clean
      - name: Format reconstructed test
        run: ruff format tests/test_pathway_specification.py
      - name: Commit verified CO completion
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Complete source-verified CO pathway specification'
          git push origin HEAD:${{ github.head_ref }}
